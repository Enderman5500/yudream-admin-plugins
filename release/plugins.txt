# Explicit tag-release module list (Maven artifactIds under yudream-plugins/).
# A protected v* tag pipeline packages and publishes only the modules listed here.
# Edit this file in the release commit; one artifactId per line, no duplicates,
# every entry must be a root-reactor module. Local and branch builds ignore this
# file and always build the full reactor.
#
# 本次发布只包含与 fork 基线（minecraft-server 1.5.1 / minecraft-activity-proof 2.4.1 /
# project-progress 1.2.2）相比真正改动的三个插件：群组服子服维度（解析、拆分统计、
# 事件流水、按子服 API）及其两个消费方。未列入的插件不打包、不发布、不跟随升版。
yudream-plugin-minecraft-server
yudream-plugin-minecraft-activity-proof
yudream-plugin-project-progress
